const sections=["summary","outcomes","manufacturers","resolution-times","follow-up-visits","replacements","owners","documents","trends","filter-options"];function headers(){return{Authorization:`Bearer ${localStorage.getItem("token")}`};}function query(filters){const q=new URLSearchParams();Object.entries(filters||{}).forEach(([k,v])=>v!==""&&v!=null&&q.set(k,v));return q;}async function parse(r){const d=await r.json().catch(()=>({}));if(!r.ok){const e=new Error(d.message||"Warranty report request failed");e.status=r.status;throw e;}return d;}export function getWarrantyReport(section,filters,signal){if(!sections.includes(section))throw new Error("Unsupported report section");return fetch(`/volt/warranty-reports/${section}?${query(filters)}`,{headers:headers(),signal}).then(parse);}export{sections};
